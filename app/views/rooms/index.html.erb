<div class="min-h-screen bg-gray-100 py-10 px-4">
  <div class="max-w-3xl mx-auto">

    <div class="flex items-center justify-between mb-6">
      <h1 class="text-3xl font-bold text-gray-800">Chat Rooms</h1>

      <%= link_to new_room_path,
          class: "bg-blue-600 hover:bg-blue-700 text-white font-medium px-4 py-2 rounded-lg shadow transition" do %>
        + Create Room
      <% end %>
    </div>

    <div class="bg-white rounded-xl shadow-md divide-y">

      <% if @rooms.any? %>
        <% @rooms.each do |room| %>

          <%= link_to room_path(room),
              class: "block px-6 py-4 hover:bg-gray-50 transition" do %>

            <div class="flex items-center justify-between">
              <div>
                <p class="text-lg font-semibold text-gray-800">
                  <%= room.name %>
                </p>
                <p class="text-sm text-gray-500">
                  Click to open chat
                </p>
              </div>

              <div class="text-blue-500 font-semibold">
                →
              </div>
            </div>

          <% end %>

        <% end %>
      <% else %>

        <div class="px-6 py-10 text-center text-gray-500">
          No rooms yet.<br>
          Create your first chat room 🚀
        </div>

      <% end %>

    </div>

  </div>
</div>
